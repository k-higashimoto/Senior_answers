<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>質問広場　新しい質問の投稿</title>

    <!-- Bootstrap core CSS -->
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet">
    <!-- Material Design Bootstrap -->
    <link href="/stylesheets/mdb.min.css" rel="stylesheet">
    <!-- Your custom styles (optional) -->
    <link href="/stylesheets/style.css" rel="stylesheet">
  </head>

  <body>
    <main>
      <div class="container z-depth-2">
        <!-- Default form register -->
        <form action="/signup" method="POST" class="text-center p-5">

          <p class="h2 mb-4">新規登録</p>

          <label class="mb-4">
            ニックネーム
            <input type="text" name="nickname" class="form-control" placeholder="ニックネーム" required />
            <small class="text-muted">ご自由にどうぞ</small>
          </label><br />
          
          <label class="mb-4">
            メールアドレス
            <input type="email" name="email" class="form-control" placeholder="メールアドレス" required />
            <small class="text-muted">あなたのメールは他の誰とも共有しません。</small>
          </label><br />
          
          <label class="mb-4">
            パスワード
            <input type="password" name="password" class="form-control" placeholder="Password" pattern="^[a-zA-Z0-9]+$" required />
            <small class="text-muted">半角英数字8字以上でお願いします</small>
          </label><br />
          
          <label class="mb-4">
            所属高専
            <select name="kosen_number" class="browser-default custom-select custom-select-lg" required>
                <option selected>所属している高専を選択</option>
                <% kosenList.forEach((kosen) => { %>
                  <option value="<%= kosen.kosen_number %>"><%= kosen.kosen_name %></option>
                <% }); %>
            </select>
          </label><br />
          
          <label class="mb-4">
              学年
              <input type="number" name="grade" min=1 max=5 class="form-control" required/>
          </label><br />


          <label class="mb-4">
            専門
            <select name="specialty_number" class="browser-default custom-select custom-select-lg mb-3" required>
                <option selected>専門を選択</option>
                <% specialtyList.forEach((specialty) => { %>
                  <option value="<%= specialty.specialty_number %>"><%= specialty.specialty_name %></option>
                <% }); %>
            </select>
          </label><br />
          
          <input type="submit" class="btn btn-info my-4 btn-block" value="新規登録"/>

      </div>
    </main>

    <script type="text/javascript" src="/javascript/jquery-3.3.1.min.js"></script>
    <!-- Bootstrap tooltips -->
    <script type="text/javascript" src="/javascript/popper.min.js"></script>
    <!-- Bootstrap core JavaScript -->
    <script type="text/javascript" src="/javascript/bootstrap.min.js"></script>
    <!-- MDB core JavaScript -->
    <script type="text/javascript" src="/javascript/mdb.min.js"></script>
  </body>
</html>
